# nuPlan Dataset Environment Variables
# Copy this file to .env and update with your actual paths
# Load in your shell: source .env or set in your ~/.bashrc / ~/.config/fish/config.fish

# ============================================================================
# REQUIRED: Dataset Locations
# ============================================================================

# Root directory containing the nuPlan dataset
# Download from: https://www.nuscenes.org/nuplan
# Contains sensor_blobs, splits, maps directories
# Approximate size: ~1300 hours of driving data (~10TB full dataset)
#export NUPLAN_DATA_ROOT="${HOME}/data/nuplan"

# Root directory containing the nuPlan maps
# Maps are provided separately from the main dataset
# Contains map files for Singapore, Las Vegas, Pittsburgh, and Boston
#export NUPLAN_MAPS_ROOT="${HOME}/data/nuplan/maps"

# Root directory for experiments, cache, and outputs
# This is where your training runs, simulation results, and cached data will be stored
# Can grow large depending on experiments - recommend dedicated drive with plenty of space
#export NUPLAN_EXP_ROOT="${HOME}/data/nuplan/exp"

# ============================================================================
# OPTIONAL: Advanced Configuration
# ============================================================================

# Number of worker processes for data loading (default: auto-detect CPUs)
# Adjust based on your system's CPU cores and memory
# export NUPLAN_NUM_WORKERS=8

# Cache size for scenario data (in GB)
# export NUPLAN_CACHE_SIZE_GB=50

# Distributed training configuration (if using multi-GPU)
# export NUPLAN_DISTRIBUTED_BACKEND="nccl"
# export NUPLAN_WORLD_SIZE=1
# export NUPLAN_RANK=0

# ============================================================================
# CUDA/GPU Configuration
# ============================================================================

# Force specific GPU devices (comma-separated device IDs)
# Useful if you have multiple GPUs and want to use specific ones
# export CUDA_VISIBLE_DEVICES=0

# PyTorch CUDA settings for better performance with Titan RTX
# export TORCH_CUDA_ARCH_LIST="7.5"  # Titan RTX compute capability

# ============================================================================
# Development Settings
# ============================================================================

# Enable debug logging for nuPlan
# export NUPLAN_DEBUG=1

# Hydra configuration overrides (for advanced users)
# export HYDRA_FULL_ERROR=1

# ============================================================================
# Example Setup for G Money's System
# ============================================================================
# Uncomment and modify these if you want a quick start

# export NUPLAN_DATA_ROOT="/mnt/storage/datasets/nuplan"
# export NUPLAN_MAPS_ROOT="/mnt/storage/datasets/nuplan/maps"
# export NUPLAN_EXP_ROOT="/mnt/storage/experiments/nuplan"
# export CUDA_VISIBLE_DEVICES=0  # Titan RTX

# ============================================================================
# Notes
# ============================================================================
#
# 1. Create directories before downloading:
#    mkdir -p $NUPLAN_DATA_ROOT $NUPLAN_MAPS_ROOT $NUPLAN_EXP_ROOT
#
# 2. Download dataset using nuplan CLI:
#    uv run nuplan_cli download
#
# 3. Verify setup:
#    just dataset-info
#
# 4. For tutorials, the mini dataset is sufficient (~50GB):
#    uv run nuplan_cli download --mini
#
